import { SpinnerOverlay, SpinnerContainer } from "./spinner.component";
const Spinner = () => {

    return (
        <SpinnerOverlay>
            <SpinnerContainer />
        </SpinnerOverlay>
    );
}

export default Spinner;